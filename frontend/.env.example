# URL base de la API. En desarrollo local con Docker, el backend
# expone el puerto 8000 al host. En producción (Railway), reemplazar
# por la URL pública del backend desplegado.
REACT_APP_API_URL=http://localhost:8000
